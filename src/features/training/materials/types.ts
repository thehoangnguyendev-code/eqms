/**
 * Training Material Types
 *
 * Re-exports from the centralised `types/material.types.ts`.
 * Keep importing from this file — backward compatible.
 */

export * from "../types/material.types";
